import React from 'react';
import {View, Text} from 'react-native';

const SmsAuthenticationScreen = (props) => {
  return (
    <View>
      <Text>SmsAuthenticationScreen</Text>
    </View>
  );
};

export default SmsAuthenticationScreen;
